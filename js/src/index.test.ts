// Basic test file to ensure npm publish works
// This can be expanded with actual tests later

describe('Basic SDK Tests', () => {
  it('should have exports', () => {
    // Basic test to ensure the module can be imported
    expect(true).toBe(true);
  });
});
